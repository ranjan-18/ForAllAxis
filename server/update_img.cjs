const mongoose = require('mongoose'); async function run() { try { await mongoose.connect('mongodb://127.0.0.1:27017/forallaxis'); const db = mongoose.connection.db; const newUrl = 'https://images.unsplash.com/photo-1539635278303-d4002c07eae3?q=80&w=800&auto=format&fit=crop'; const result = await db.collection('projects').updateOne({ slug: 'triply-group-expense-tracker' }, { $set: { 'images.0.url': newUrl, 'thumbnail.url': newUrl } }); console.log('Updated:', result.modifiedCount); process.exit(0); } catch (err) { console.error(err); process.exit(1); } } run();
